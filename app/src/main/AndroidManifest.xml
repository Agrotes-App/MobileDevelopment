<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- internet permission -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Location permission -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- camera permission -->
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" /> <!-- storage permission -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Agrotesmobile"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.password.PasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.diseaseOption.DiseaseOptionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.detailDisease.DetailDiseaseActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.detailHistory.DetailHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.editProfile.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.prediction.PredictionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.camera.CameraActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.welcome.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.signup.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>